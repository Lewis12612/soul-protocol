# V3.8.6 架构速查

> 灵魂系统 V3.8.6 — 模块化 + hub分发

---

## 核心数据

| 指标 | 值 |
|------|-----|
| hook行数 | 213行（原1229行） |
| 简化度 | 82.7% |
| 源文件数 | 20个.ts |
| 模块数 | 5场景 + 1监控 |

---

## 调用流程

```
before-prompt-build.ts
  → hub.dispatch(context)
    → startup | heartbeat | recovery | hardcoded
  → protocol.build()
  → prependSystemContext()
```

---

## 模块速查

| 模块 | 触发 | 输出 |
|------|------|------|
| startup | 首次 | 全量记忆注入 |
| heartbeat | 定时 | 增量 + 日志 |
| recovery | compact后 | 上下文恢复 |
| hardcoded | 定时任务 | 自动化操作 |
| memory | 按需 | L2/L3读取 |
| monitor | 按需 | stats + health |

---

## 关键命令

```bash
# 编译
npm run build

# 产物位置
dist/hub.js, dist/protocol.js, dist/modules/*.js

# 日志位置
logs/heartbeat-YYYYMMDD.log
```

---

## 导出入口

```typescript
// modules
import { runStartup, runHeartbeat, runRecovery } from "./modules/index.js";

// monitor
import { collectStats, checkModuleHealth } from "./monitor/index.js";

// protocol
import { buildProtocol } from "./protocol.js";
```
